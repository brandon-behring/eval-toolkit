# `eval_toolkit.stacking`

Detector stacking via the {class}`MetaLearner` Protocol (v0.45 reference
impl: {class}`LogisticStacker`). Combine multiple binary scorers'
calibrated outputs into a single P(positive) estimate on a held-out
stacking set.

```{eval-rst}
.. currentmodule:: eval_toolkit.stacking

.. autosummary::
   :toctree: generated/stacking/
   :nosignatures:

   LogisticStacker
   MetaLearner
```
